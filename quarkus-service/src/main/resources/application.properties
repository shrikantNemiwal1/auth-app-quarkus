# Database config
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=postgres
quarkus.datasource.password=postgres
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/authdb

quarkus.hibernate-orm.database.generation=none

# Flyway settings
quarkus.flyway.migrate-at-start=true
quarkus.flyway.clean-at-start=false
quarkus.flyway.baseline-on-migrate=true

# OpenAPI path
quarkus.smallrye-openapi.path=/openapi

# Swagger UI path
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui

# SMTP config
quarkus.mailer.from=shrikant.nemiwal3@gmail.com
quarkus.mailer.host=smtp.gmail.com
quarkus.mailer.port=587
quarkus.mailer.username=shrikant.nemiwal3@gmail.com
quarkus.mailer.password=lklplrenakqzqrug
quarkus.mailer.start-tls=REQUIRED
quarkus.mailer.ssl=false
quarkus.mailer.auth-methods=LOGIN PLAIN

# CORS configuration for Node.js communication
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:3000
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.methods=GET,PUT,POST,DELETE,OPTIONS
quarkus.http.cors.access-control-allow-credentials=true

# Application URLs
app.frontend-url=http://localhost:3000
app.internal.allowed-origins=http://localhost:3000

# Remove JWT config entirely for Node.js session management
